each article in articles 
    .article   
        h3 #{article.name}
        p #{article.body.slice(0, 100)}...
        a.btn.btn-outline(
            href=`/articles/${article.id}`
        ) View Details 
